# Launch Blockers

Current decision: **BLOCKED — NOT PUBLISH-READY**

This register contains conditions that prevent a production release. A row closes only when objective evidence is linked from the Evidence column and the release owner accepts it. Development may proceed around an external dependency, but a provider-dependent feature may not be faked or silently disabled in production.

| ID | Blocking condition | Required resolution/evidence | Status |
| --- | --- | --- | :---: |
| LB-001 | A complete reproducible cross-platform product runtime does not exist. | Run the Docker-backed stack and migrations, native Android/iOS builds, both web deployments, CI, and infrastructure from clean supported hosts with retained evidence. | Open |
| LB-002 | The auth-to-answer code slice lacks real production pre-traffic acceptance. | The isolated local real-service Android path passes; under ADR-018 there is no persistent cloud staging. Retain the blocker until a no-public-traffic production canary covers native sign-in, course access, answer submission, backend score/energy, idempotent retry, outbox projection, and mobile display. | Open |
| LB-003 | Owner P0 product/legal/platform decisions are unresolved. | Relevant rows in `docs/OWNER_ACTIONS.md` are approved and recorded in contracts/ADRs where needed. | Open |
| LB-004 | Identity and account lifecycle are not production-integrated. | Managed OIDC, recovery, multi-device refresh rotation, export, deletion, and audited support behavior pass integration/security tests. | Open |
| LB-005 | Local/test intake, approval, Type-D composition, client-capability enforcement, idempotent initial and subsequent draft creation, impact-bound publication/rollback, release-aware reprojection, a resumable intake-to-publication operator, and a narrow one-question editor are implemented; production enablement and the complete editor remain absent. | Retain evidence for checksum-bound multipart upload, exact object-version recovery, SQS/DLQ worker execution, private malware scanning, immutable quarantine/archive/report provenance, owner-scoped no-store preview/issues/import discovery, HMAC-bound pagination, state-loss resumption and prior-activation suppression, approval binding, V13 draft/source-lineage/runtime-option/activation/subsequent-authoring constraints, deterministic Type-D composition, exact stored release capabilities, validation/malware failures, exactly-once draft creation, strong ETag/If-Match, secure unsaved recovery, explicit old/mine/latest conflict resolution, separate mobile confirmation gates, identifiers-only publication/draft outbox facts, and non-empty publication/rollback reprojection with unchanged lifetime score. Close only after author eligibility and versioned UGC/legal consent, least-privilege API/worker IAM and separate database roles, private scanner policy, KMS/Object Lock/retention/legal-hold evidence, production course-tree editor and multi-entity diff/reapply, large-page retry/dead-job/cache-invalidation evidence, and the same controls pass locally plus the ADR-018 production pre-traffic canary. | Open |
| LB-006 | Production online learning integrity remains unproven. | Local real-PostgreSQL A/B/C/D transaction, replay, privacy, and answer-key isolation evidence exists; retain this blocker until production pre-traffic/device-matrix/concurrency/load/fraud evidence covers scoring, energy, completion, revision and support-language pinning, complete-bijection/all-or-nothing Type-D grading, duplicate/replay behavior, lifetime monotonicity, no explicit submitted/correct mapping or correct-pair count in durable clients/facts/events/diagnostics, and database-only candidate-testing resistance from random-salt versioned HMAC evidence with an external keyring and constant-time replay. Accept and test the residual authoritative `is_correct` disclosure, including that a false two-pair result identifies the sole swap. | Open |
| LB-007 | Offline-package isolation is unproven. | Signature/hash, authorization, atomic install, entitlement lock, version check, and proof that answer history cannot reach scored APIs. | Open |
| LB-008 | Store commerce and entitlement lifecycle are not operational. | Course-specific products active in both stores; sandbox then production verification, restore, RTDN/ASSN V2, refund/void/chargeback, reconciliation, and revocation tests. | Open |
| LB-009 | Rewarded advertising is not production-verified. | AdMob SSV signature verification, replay protection, consent/ATT policy, delayed callback behavior, abuse controls, and production ad-unit configuration. | Open |
| LB-010 | Marketplace earnings, KYC/KYB, tax, and payout are unresolved. | Real provider integration, append-only earnings/reversal ledger, financial reconciliation, holds/review, provider webhook tests, and approved marketplace/legal policy. | Open |
| LB-011 | UGC, public profile, privacy, child safety, and moderation controls are incomplete. | Approved policies plus reporting, blocking, review, appeal, takedown, scraping/rate limits, privacy requests, and audited admin workflows. | Open |
| LB-012 | The ADR-018 production application environment and secure release path are incomplete. | Protected state/OIDC and the application foundation are applied without drift (`docs/evidence/production-bootstrap-2026-08-04.md`, `docs/evidence/production-deployment-2026-08-04.md`). The guarded 2026-08-10 internal-test activation promoted one scanned immutable API task, applied Flyway V14, passed database-aware readiness and Cognito Android-redirect connectivity, and finished with a no-change digest-pinned plan (`docs/evidence/production-internal-test-activation-2026-08-10.md`). Existing evidence also covers the guarded account/region, no-NAT runtime, private encrypted Single-AZ PostgreSQL with PITR/deletion protection, versioned encrypted import buckets, queue/DLQ shells, separate IAM/database identities, Secrets Manager, confirmed operations alerts, USD 50 budget/cost governor, and passing same-account database/application restore (`docs/evidence/production-database-restore-2026-08-04.md`). Retain this blocker until TLS custom edge/WAF, production worker/private scanner execution, Object Lock/retention/legal-hold acceptance, replay-key rotation/rollback and historical retention, supported independent approval, the full native auth-to-answer canary, and rollback rehearsal are evidenced. | Open |
| LB-013 | Observability, incident response, and operational ownership are absent. | Critical-path SLO dashboards, alerts, trace/log/metric correlation, redaction evidence, runbooks, escalation owners, and an incident exercise. | Open |
| LB-014 | Backup and disaster-recovery custody remains incomplete. | The documented same-account PITR exercise passed with 5m06s RPO lag and 1h05m42s end-to-end application RTO (`docs/evidence/production-database-restore-2026-08-04.md`). Retain this blocker until backup/retention policy and ownership are approved, isolated/cross-region copies and ledger exports are implemented, and combined recovery evidence meets the approved targets. | Open |
| LB-015 | Security and supply-chain gates are incomplete. | Threat models, ASVS/MASVS review, authorization tests, secret scan, dependency/license scan, signed SBOM/artifacts, penetration findings closed, and no critical/high vulnerability. | Open |
| LB-016 | Accessibility, localization, RTL, real-device, and performance release gates are unproven. | The repository-managed API 24/30/36 emulator smoke matrix passes 15/15; retain this blocker until supported physical-device/update/migration/background coverage plus automated/manual crash, startup, latency, jank, dynamic type, screen reader, contrast, touch-target, and RTL budgets pass. | Open |
| LB-017 | Legal/public routes are gated and contain no approved publishable content. | Reachable HTTPS privacy, terms, support, community, copyright, and account deletion/export pages with owner-approved content plus accurate store privacy/data-safety declarations. | Open |
| LB-018 | Signed release artifacts and store approvals are absent. | Production-signed AAB, TestFlight archive, store metadata/review evidence, active IAP products, staged rollout plan, reviewer access, and signing custody verification. | Open |
| LB-019 | Final release evidence is incomplete. | Every mandatory item in `docs/RELEASE_CHECKLIST.md` is checked with evidence, no blocking defect remains, and the accountable owner records go/no-go. | Open |

If all engineering work is complete but an owner account, provider approval, legal decision, or store review remains unresolved, the correct status is still **BLOCKED — NOT PUBLISH-READY**.
